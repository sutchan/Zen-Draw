# 2026-06-26

## UI 审查 (ardot-design-assistant skill)

全面审查了 ZenDraw 应用 UI（7 个核心模块：page.tsx, layout.tsx, draw-display.tsx, draw-button.tsx, history-list.tsx, settings-panel.tsx, number-roller.tsx）。

综合评分：78/100

### 高优问题
1. `settings-panel.tsx:724` — History 标签页空壳，历史记录未在面板内渲染
2. `settings-panel.tsx:314–338` — AppearanceSettings 外观分区 displayT 硬编码中文，不跟随 language prop
3. `draw-button.tsx:126` — 按钮 text-white 在浅色高亮主题下对比度不足，应改为 text-primary-foreground

### 中优问题
4. HistoryCard role="article" 语义与点击行为不匹配
5. DrawDisplay pt-14 + py-12 双重上边距叠加（合计 104px）
6. ResultCard rounded-[2.5rem] 硬编码，脱离 --radius 变量
7. SidebarToggle/HeaderBar 在 SheetContent 内是死代码（被 Sheet 自带关闭按钮遮盖）
8. error 状态在 DrawDisplay 中未渲染

### 优势
- ARIA 覆盖完整，prefers-reduced-motion 支持，键盘快捷键完善
- 主题系统设计良好（10 主题 + 3 字体 + 深浅模式）

### 优化实施 (15:31)

全部 6 项修复已完成：

**高优修复：**
1. `draw-button.tsx:125` — `text-white` → `text-primary-foreground`（基础状态），drawing 状态保留 `text-white`
2. `settings-panel.tsx` — AppearanceSettings 添加 `language` prop，displayT/presetLabels 中英双语化；CustomListSettings 同步修复 Dialog 内写死中文
3. `settings-panel.tsx` — History 标签页现渲染真实 `<HistoryList>` 组件，通过 SettingsPanelProps 新增 `history`/`onClearHistory` 传入

**中优修复：**
4. `draw-display.tsx` — 移除外层 `pt-14`、内层 `py-12→py-4`，消除双重上边距叠加
5. `draw-display.tsx` — 新增 `ErrorScreen` 子组件（AlertTriangle 图标 + 双语提示），AnimatePresence 中优先检测 `status === "error"`
6. `draw-display.tsx` — ResultCard `rounded-[2.5rem]` → `rounded-[calc(var(--radius)*3.33)]` 绑定主题设计系统
7. `history-list.tsx` — HistoryCard `role="article"` → `role="button"` + `tabIndex={0}`

**联动修改：**
- `page.tsx` — 向 SettingsPanel 传入 `history={draw.history}` / `onClearHistory={draw.clearHistory}`
- `settings-panel.tsx` — 导入 HistoryList/HistoryEntry 类型

关于审查#7（SidebarToggle/HeaderBar死代码）：该判断有误，这两个组件在 `<motion.aside>` 外部渲染，作为页面级 chrome 正常运行，无需修改。
